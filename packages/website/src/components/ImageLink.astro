---
export interface Props {
  src: string;
  href: string;
  alt: string;
}

const { href, src, alt } = Astro.props;
---

<a href={href} class="flex">
  <img src={src} alt={alt} class="object-contain max-w-[140px] max-h-[80px]" />
</a>
