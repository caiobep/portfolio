---
import { ViewTransitions } from 'astro:transitions'
import LinkButton from '../components/LinkButton.astro'
import Typography from '../components/Typography.astro'
import Footer from '../components/Footer.astro'

export interface Props {
  title: string
}

const { title, ...rest } = Astro.props
---

<!DOCTYPE html>
<html lang="en" class="dark:bg-black bg-gray/50 dark:text-gray-300">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="theme-color" content="currentColor" />
  <meta name="generator" content={Astro.generator} />
  <link rel="icon" type="image/svg+xml" href="favicon.svg" />
  <link rel="mask-icon" href="favicon.svg" color="#FFF" />
  <title>{title}</title>
  <ViewTransitions />
</head>

<body class="px-6 sm:max-w-[90%] md:px-[4rem] max-w-[104rem] mx-auto transition-all duration-300">
  <navbar class="py-4 flex flex-row justify-between dark:text-gray-300 text-gray-900 font-medium">
    <header class="pt-2 text-xl dark:hover:text-white">
      <a href="/" class="animate__animated animate__bounceInLeft">
        Caio Amaral
      </a>
    </header>

    <ul class="flex flex-row gap-1 animate__animated animate__fadeInRight">
      <li>
        <LinkButton
            href="https://www.notion.so/caicorre/Projects-cfd6734a46ea4a038364fd596c428435?pvs=4"
            title="Projects" />
      </li>
      <li>
        <LinkButton href="/posts" title="Posts" />
      </li>
    </ul>
  </navbar>


  <main slot="main" {...rest}>
    <slot />
  </main>

  <Footer />

  <style is:global>

    @keyframes slideUpFadeIn {
      0% {
        opacity: 0;
        transform: translateY(1rem);
      }

      100% {
        opacity: 1;
        transform: translateY(0);
      }
    }

    body {
      display: flex;
      flex-direction: column;
      min-height: 100vh;
    }

    .color-secondary {
      /* @apply dark: text-white/60 text-black/60; */
    }

    @media (prefers-color-scheme: dark) {
      html {
        background-color: #fff;
      }
    }
  </style>


</body>

</html>
